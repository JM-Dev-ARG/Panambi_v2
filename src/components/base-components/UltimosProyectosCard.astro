---
interface Props {
    titulo: string;
    bgImg: string;
    cardLetra: string
    bgColor?: string;
   
}

const { titulo, bgImg, cardLetra, bgColor  } = Astro.props;
---

<div
  class=`group relative flex w-full h-[300px] cursor-pointer items-center justify-center overflow-hidden rounded-3xl border-2 text-2xl z-[4] border-negro  text-neutral-100 shadow-Panambi transition-all duration-500 ease-in-out  ${bgColor+"/0"} ${cardLetra} hover:scale-75`
>
  <!-- <div
    class="absolute inset-0  bg-contain bg-center bg-no-repeat transition-all duration-300 ease-in-out group-hover:scale-[1.2]"
    style=`background-image: url('${bgImg}');`
  >
  </div>
  <p class="z-[4] backdrop-blur-xl p-4 rounded-xl ">{titulo}</p> -->
</div>

<style>
.a {
    grid-area: a;
    
  }
  .b {
    grid-area: b;
  }
  .c {
    grid-area: c;
  }
  .d {
    grid-area: d;
  }</style>